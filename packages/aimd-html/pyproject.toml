[project]
name = "aimd-html"
version = "0.9.1"
description = "Defuddle-backed HTML extraction helpers for aimd workflows."
authors = [
    { name = "Shu Li", email = "zetarylee@gmail.com" }
]
requires-python = ">=3.10, <3.13"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.9,<0.9.0"]
build-backend = "uv_build"
